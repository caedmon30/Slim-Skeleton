{% extends "./base.html.twig" %}

{% block head %}
    {{ parent() }}

{% endblock %}
    {% block styles %}
    {% endblock %}
    {% block top_javascript %}

    {% endblock %}
{% block content %}
    {% include './partials/inner_navbar.html.twig' %}
    {% set page_title = 'Administration' %}
    <div id="GridContainer" class="container mx-auto p-2.5">
        {% include './partials/breadcrumb.html.twig' %}

        <h1 class="mb-6 font-extrabold leading-none tracking-tight text-gray-900 md:text-xl lg:text-3xl dark:text-slate-700">System <span class="text-red-600 dark:text-red-500">Administration</span></h1>
        <section class="text-gray-600 body-font">
            <div class="container px-5 py-4 mx-auto flex flex-wrap">
                <div class="flex flex-wrap -m-4">
                    <div class="p-4 lg:w-1/2 md:w-full">
                        <div class="flex border-2 rounded-lg border-gray-200 border-opacity-50 p-8 sm:flex-row flex-col">
                            <div class="w-16 h-16 sm:mr-8 sm:mb-0 mb-4 inline-flex items-center justify-center rounded-full bg-gray-100 text-red-500 flex-shrink-0">
                                <i class="fa-solid fa-users-medical text-2xl"></i>
                            </div>
                            <div class="flex-grow">
                                <h2 class="text-gray-900 text-lg title-font font-medium mb-3 text-nowrap">System Users</h2>

                                <a href="users" class="mt-3 text-red-500 inline-flex items-center">View Task

                                </a>
                            </div>
                        </div>
                    </div>
                    <div class="p-4 lg:w-1/2 md:w-full">
                        <div class="flex border-2 rounded-lg border-gray-200 border-opacity-50 p-8 sm:flex-row flex-col">
                            <div class="w-16 h-16 sm:mr-8 sm:mb-0 mb-4 inline-flex items-center justify-center rounded-full bg-gray-100 text-red-500 flex-shrink-0">
                                <i class="fa-solid fa-users text-2xl"></i>
                            </div>
                            <div class="flex-grow">
                                <h2 class="text-gray-900 text-lg title-font font-medium mb-3 text-nowrap">Employee Types</h2>

                                <a href="employee-status" class="mt-3 text-red-500 inline-flex items-center">View Task

                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <p>To add an employee type to the database, click the plus button below to access the employee type (status) management tool, navigate to the entry form and provide employee type in clearly understandable language</p>
        <div id="dataGridEmployees" class="dark:!bg-gray-100 p-4 mb-8 mt-4 rounded-md"></div>
    </div>

{% endblock %}

{% block javascript %}
<script src="{{ '../assets/js/users.js' }}"></script>
<script src="{{ '../assets/js/employees.js' }}"></script>
{% endblock %}