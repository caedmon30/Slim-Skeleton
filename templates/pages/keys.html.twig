{% extends "./base.html.twig" %}

{% block head %}
    {{ parent() }}

{% endblock %}
    {% block styles %}
    {% endblock %}
    {% block top_javascript %}

    {% endblock %}
{% block content %}
    {% include './partials/inner_navbar.html.twig' %}

    {% set page_title = 'Manage Keys' %}
    {% include './partials/breadcrumb.html.twig' %}

    <div id="keyGridContainer" class="container mx-auto p-2.5 min-h-full">

        <h1 class="mb-4 text-4xl font-extrabold leading-none tracking-tight text-gray-900 md:text-5xl lg:text-6xl dark:text-slate-700">Manage <span class="text-red-600 dark:text-red-500">Keys</span></h1>
        <p class="text-lg font-medium text-gray-600 lg:text-lg dark:text-gray-900 mb-4">Register, assign, and reassign keys</p>
        <p>To add a key to the database, click the plus button below to access the key management tool, navigate to the entry form, and provide the requested in formation. Required fields are marked with an asterisk (*).</p>

        <div id="dataGridKeys" class="dark:!bg-gray-100 pt-4 px-6 pb-12 mt-8 mb-8 rounded-md"></div>
    </div>
    {% endblock %}

    {% block javascript %}
    <script src="{{ '../assets/js/keys.js' }}"></script>
{% endblock %}